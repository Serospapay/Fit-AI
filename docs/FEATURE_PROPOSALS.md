# 💡 Пропозиції функціоналу для покращення проекту

**Дата**: 2024-11-04

---

## 🎯 Пріоритетні функції (що можна реалізувати швидко)

### 1. Система цілей та відстеження прогресу

**Опис**: Користувач може ставити конкретні цілі (наприклад, "втратити 5 кг за 3 місяці", "підняти жим лежачи до 100 кг") і відстежувати прогрес.

**Що потрібно**:
- Нова таблиця `Goal` в Prisma:
  - `id`, `userId`, `title`, `description`, `targetValue`, `currentValue`, `startDate`, `targetDate`, `status`, `category` (weight, strength, endurance, nutrition)
- Backend API:
  - `POST /api/goals` - створити ціль
  - `GET /api/goals` - отримати всі цілі
  - `PUT /api/goals/:id` - оновити прогрес
  - `DELETE /api/goals/:id` - видалити ціль
- Frontend:
  - Сторінка `/goals` зі списком цілей
  - Форма створення нової цілі
  - Візуалізація прогресу (прогрес-бари, графіки)
  - Автоматичне оновлення прогресу на основі тренувань/ваги

**Час реалізації**: 2-3 години

---

### 2. Нагадування про тренування

**Опис**: Система нагадувань про заплановані тренування через браузерні нотифікації.

**Що потрібно**:
- Нова таблиця `Reminder` в Prisma:
  - `id`, `userId`, `type` (workout, nutrition), `time`, `daysOfWeek`, `enabled`, `message`
- Backend API:
  - CRUD операції для нагадувань
- Frontend:
  - Сторінка `/reminders` або секція в профілі
  - Використання Browser Notification API
  - Налаштування часу та днів тижня

**Час реалізації**: 2-3 години

---

### 3. Інтелектуальні рекомендації на основі статистики

**Опис**: Система аналізує дані користувача та дає персоналізовані поради.

**Приклади рекомендацій**:
- "Ви не тренувалися 3 дні підряд. Час повернутися до залу!"
- "Ваше споживання білків нижче за рекомендоване. Додайте більше білкових продуктів."
- "Ви досягли серії з 7 днів тренувань підряд! Продовжуйте в тому ж дусі!"
- "Ваша середня тривалість тренувань збільшилася на 15%. Відмінна робота!"

**Що потрібно**:
- Нова таблиця `Recommendation` в Prisma:
  - `id`, `userId`, `type`, `title`, `message`, `priority`, `isRead`, `createdAt`
- Backend API:
  - `GET /api/recommendations` - отримати рекомендації
  - `POST /api/recommendations/generate` - згенерувати нові на основі статистики
  - `PUT /api/recommendations/:id/read` - позначити як прочитане
- Frontend:
  - Бейдж з кількістю нових рекомендацій у навігації
  - Сторінка `/recommendations` або dropdown в навігації
  - Картки з рекомендаціями та можливістю позначати як прочитані

**Час реалізації**: 3-4 години

---

### 4. Експорт даних (PDF/Excel звіти)

**Опис**: Користувач може експортувати свої дані у зручному форматі для аналізу або зберігання.

**Що потрібно**:
- Backend:
  - `GET /api/export/workouts?format=pdf|excel&startDate=&endDate=`
  - `GET /api/export/nutrition?format=pdf|excel&startDate=&endDate=`
  - Використання бібліотек: `pdfkit` або `jspdf` для PDF, `exceljs` для Excel
- Frontend:
  - Кнопка "Експорт" на сторінках тренувань/харчування
  - Вибір формату та періоду
  - Автоматичне завантаження файлу

**Час реалізації**: 4-5 годин

---

### 5. Фото прогресу (до/після)

**Опис**: Користувач може завантажувати фото для відстеження візуального прогресу.

**Що потрібно**:
- Нова таблиця `ProgressPhoto` в Prisma:
  - `id`, `userId`, `url`, `date`, `notes`, `weight` (на момент фото), `bodyPart` (optional)
- Backend:
  - Завантаження файлів (multer або cloudinary)
  - API для CRUD операцій
- Frontend:
  - Сторінка `/progress-photos` з галереєю
  - Завантаження фото з датою та нотатками
  - Порівняння "до/після"

**Час реалізації**: 4-5 годин

---

### 6. Шаблони тренувань (Templates)

**Опис**: Користувач може зберегти улюблене тренування як шаблон і швидко використовувати його знову.

**Що потрібно**:
- Нова таблиця `WorkoutTemplate` в Prisma:
  - `id`, `userId`, `name`, `description`, `type`, `exercises` (JSON)
- Backend API:
  - CRUD для шаблонів
  - `POST /api/workouts/from-template/:templateId` - створити тренування з шаблону
- Frontend:
  - Секція "Мої шаблони" на сторінці створення тренування
  - Можливість зберегти поточне тренування як шаблон
  - Швидке додавання тренування з шаблону

**Час реалізації**: 3-4 години

---

### 7. Календар тренувань з плануванням

**Опис**: Візуальний календар, де користувач може планувати майбутні тренування та бачити минулі.

**Що потрібно**:
- Frontend:
  - Бібліотека календаря (react-big-calendar або custom)
  - Можливість перетягування тренувань
  - Планування майбутніх тренувань (з status='planned')
- Backend:
  - Додати поле `status` до таблиці `Workout` (completed, planned, cancelled)
  - API для оновлення статусу

**Час реалізації**: 3-4 години

---

### 8. Система мотиваційних цитат та фактів

**Опис**: Щоденна мотиваційна цитата або цікавий факт про фітнес на Dashboard.

**Що потрібно**:
- База даних цитат (можна seed файл з 50-100 цитатами)
- API endpoint для отримання випадкової цитати
- Компонент на Dashboard з щоденною цитатою

**Час реалізації**: 1-2 години

---

### 9. Порівняння з попередніми тренуваннями

**Опис**: При додаванні вправи система показує, скільки ви робили в минулий раз.

**Що потрібно**:
- При додаванні вправи в тренування:
  - Пошук останнього разу, коли ця вправа виконувалася
  - Показ порівняння (наприклад, "Останній раз: 3x10 з 80кг")
  - Візуальне виділення покращень

**Час реалізації**: 2-3 години

---

### 10. Інтеграція з календарем (ICS файли)

**Опис**: Експорт запланованих тренувань у формат ICS для імпорту в Google Calendar, Outlook тощо.

**Що потрібно**:
- Backend API endpoint для генерації ICS файлу
- Кнопка "Додати в календар" на сторінці тренувань

**Час реалізації**: 1-2 години

---

## 🚀 Рекомендований порядок реалізації

### Етап 1 (Швидкі перемоги - 1-2 дні):
1. ✅ Система мотиваційних цитат (1-2 год)
2. ✅ Порівняння з попередніми тренуваннями (2-3 год)
3. ✅ Шаблони тренувань (3-4 год)

### Етап 2 (Корисні функції - 2-3 дні):
4. ✅ Система цілей та відстеження прогресу (2-3 год)
5. ✅ Інтелектуальні рекомендації (3-4 год)

### Етап 3 (Розширені функції - 3-4 дні):
6. ✅ Експорт даних PDF/Excel (4-5 год)
7. ✅ Нагадування про тренування (2-3 год)
8. ✅ Календар тренувань (3-4 год)

### Етап 4 (Опціонально):
9. ⏳ Фото прогресу (4-5 год)
10. ⏳ Інтеграція з календарем (1-2 год)

---

## 💡 Додаткові ідеї для майбутнього

- **Соціальні функції**: Поділитися досягненнями, публічний профіль
- **Челленджі**: Тижневі/місячні виклики з нагородами
- **Інтеграція з носими пристроями**: Apple Health, Google Fit
- **AI-тренер**: Чат-бот з порадами на основі ML
- **Рецепти**: База рецептів з розрахунком БЖУ
- **Водний баланс**: Відстеження споживання води
- **Сон**: Логування та аналіз сну

---

**Які функції ви хочете реалізувати першими?**

Рекомендую почати з:
1. Шаблони тренувань (дуже корисно для щоденного використання)
2. Система цілей (додає мотивацію та зміст)
3. Інтелектуальні рекомендації (робить проект "живим")

