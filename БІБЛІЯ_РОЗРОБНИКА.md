# 📖 Біблія розробника

**Шпаргалка для правильного старту нового проекту**

## 🎯 Перед початком розробки

### 1. Створення проєкту
```bash
# Frontend (Next.js)
npx create-next-app@latest project-name --typescript

# Backend (Node.js)
mkdir project-name
cd project-name
npm init -y
npm install express cors dotenv
npm install -D typescript @types/node @types/express @types/cors ts-node nodemon
```

### 2. Підключення Git
```bash
git init
git add .
git commit -m "initial commit"
git remote add origin https://github.com/username/repo.git
git branch -M main
git push -u origin main
```

### 3. Створити `.gitignore`
```
node_modules/
.env
.env.local
.next/
dist/
build/
logs/
*.log
```

---

## 🗄️ База даних (PostgreSQL + Prisma)

### 1. Встановити Prisma
```bash
npm install prisma @prisma/client
npm install -D prisma
```

### 2. Ініціалізувати Prisma
```bash
npx prisma init
```

### 3. Налаштувати `.env`
```env
DATABASE_URL="postgresql://user:password@localhost:5432/dbname?schema=public"
PORT=5000
```

### 4. Створити міграції
```bash
npx prisma migrate dev --name init
npx prisma generate
```

### 5. **ВАЖЛИВО:** Seed дані
```bash
# package.json
"prisma": { "seed": "ts-node prisma/seed.ts" }

# seed.ts
import { PrismaClient } from '@prisma/client';
const prisma = new PrismaClient();
```

---

## 🔐 Автентифікація (якщо потрібна)

### 1. Встановити пакети
```bash
npm install jsonwebtoken bcrypt
npm install -D @types/jsonwebtoken @types/bcrypt
```

### 2. Створити middleware/auth.ts
```typescript
import { Request, Response, NextFunction } from 'express';
import jwt from 'jsonwebtoken';

export interface AuthRequest extends Request {
  userId?: string;
}

export const authenticateToken = (req: AuthRequest, res: Response, next: NextFunction) => {
  // Реалізація перевірки токену
};
```

---

## 📝 Логування та помилки

### 1. Встановити Winston
```bash
npm install winston
```

### 2. Створити logger.ts
```typescript
import winston from 'winston';
import fs from 'fs';
const logDir = 'logs';
if (!fs.existsSync(logDir)) fs.mkdirSync(logDir, { recursive: true });

const logger = winston.createLogger({
  level: 'info',
  format: winston.format.combine(
    winston.format.timestamp(),
    winston.format.errors({ stack: true }),
    winston.format.json()
  ),
  transports: [
    new winston.transports.File({ filename: 'logs/error.log', level: 'error' }),
    new winston.transports.File({ filename: 'logs/combined.log' }),
  ],
  exceptionHandlers: [
    new winston.transports.File({ filename: 'logs/exceptions.log' })
  ],
  rejectionHandlers: [
    new winston.transports.File({ filename: 'logs/rejections.log' })
  ],
});

if (process.env.NODE_ENV !== 'production') {
  logger.add(new winston.transports.Console({
    format: winston.format.combine(
      winston.format.colorize(),
      winston.format.simple()
    )
  }));
}

export default logger;
```

### 3. Створити errorHandler.ts
```typescript
import { Request, Response, NextFunction } from 'express';
import logger from './logger';

export class AppError extends Error {
  statusCode: number;
  isOperational: boolean;

  constructor(message: string, statusCode: number) {
    super(message);
    this.statusCode = statusCode;
    this.isOperational = true;
  }
}

export const errorHandler = (err: Error, req: Request, res: Response, next: NextFunction) => {
  logger.error('Error:', { message: err.message, stack: err.stack });
  
  if (err instanceof AppError) {
    return res.status(err.statusCode).json({ error: err.message });
  }
  
  res.status(500).json({ error: 'Internal Server Error' });
};
```

### 4. **ВАЖЛИВО:** Graceful Shutdown
```typescript
const gracefulShutdown = (signal: string) => {
  logger.info(`Received ${signal}. Shutting down...`);
  server.close(() => process.exit(0));
  setTimeout(() => process.exit(1), 10000);
};

process.on('SIGTERM', () => gracefulShutdown('SIGTERM'));
process.on('SIGINT', () => gracefulShutdown('SIGINT'));
```

---

## 🔄 Prisma - Обробка помилок підключення

### Створити prisma.ts з автоматичним перепідключенням
```typescript
import { PrismaClient } from '@prisma/client';
import logger from './logger';

export const prisma = new PrismaClient();

prisma.$connect()
  .then(() => logger.info('✅ Database connected'))
  .catch((error) => logger.error('❌ Database connection failed:', error));

process.on('beforeExit', async () => {
  await prisma.$disconnect();
  logger.info('📦 Prisma disconnected');
});

process.on('uncaughtException', async (error) => {
  logger.error('❌ Uncaught Exception:', error);
  await prisma.$disconnect();
  process.exit(1);
});

process.on('unhandledRejection', async (reason: any) => {
  logger.error('❌ Unhandled Rejection:', reason);
  await prisma.$disconnect();
  process.exit(1);
});
```

---

## 🎨 Frontend - Основні правила

### 1. Встановити залежності
```bash
npm install bootstrap bootstrap-icons
# або для Tailwind
npm install tailwindcss postcss autoprefixer
```

### 2. Налаштувати API клієнт
```typescript
const API_URL = process.env.NEXT_PUBLIC_API_URL || 'http://localhost:5000/api';

export const api = {
  get: (path: string) => fetch(`${API_URL}${path}`).then(r => r.json()),
  post: (path: string, data: any) => 
    fetch(`${API_URL}${path}`, {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(data)
    }).then(r => r.json()),
};
```

### 3. **ВАЖЛИВО:** Error boundaries та обробка помилок
```typescript
'use client';

try {
  const data = await api.get('/endpoint');
} catch (error) {
  console.error('Error:', error);
  // Показувати користувачу user-friendly повідомлення
}
```

---

## 📦 Nodemon конфігурація

### nodemon.json
```json
{
  "watch": ["src"],
  "ext": "ts,json",
  "exec": "ts-node src/index.ts",
  "ignore": ["src/**/*.spec.ts", "node_modules"]
}
```

---

## ✅ Чек-лист перед деплоєм

### Backend
- [ ] Встановлено та налаштовано Prisma
- [ ] Створено міграції БД
- [ ] Додано Seed дані
- [ ] Налаштовано Winston логування
- [ ] Додано Error handling middleware
- [ ] Реалізовано Graceful shutdown
- [ ] Додано DB reconnection logic
- [ ] Створено `.env.example`
- [ ] Додано middleware для CORS
- [ ] Налаштовано Health check endpoint

### Frontend
- [ ] Налаштовано API клієнт
- [ ] Додано Error boundaries
- [ ] Додано Loading стани
- [ ] Перевірено responsive дизайн
- [ ] Налаштовано Environment variables
- [ ] Додано Form validation
- [ ] Налаштовано роутинг

### Git та DevOps
- [ ] Створено `.gitignore`
- [ ] Підключено GitHub
- [ ] Створено `.env.example`
- [ ] Додано README з інструкціями
- [ ] Налаштовано CI/CD (якщо потрібно)

---

## 🚀 Полезні команди

### Prisma
```bash
npx prisma generate        # Генерувати клієнт
npx prisma migrate dev     # Створити міграцію
npx prisma migrate deploy  # Застосувати міграції (production)
npx prisma studio          # Відкрити Prisma Studio
npx prisma db seed         # Запустити seed
```

### Git
```bash
git add -A                 # Додати всі зміни
git commit -m "message"    # Закомітити
git push                   # Запушити
git status                 # Перевірити статус
git log --oneline -10      # Останні коміти
```

### NPM
```bash
npm install               # Встановити залежності
npm run dev              # Запустити dev сервер
npm run build            # Білдити проект
npm run start            # Запустити production
npm audit                # Перевірити безпеку
```

---

## 🎯 Best Practices

### Backend
1. ✅ Завжди логувати помилки
2. ✅ Використовувати TypeScript
3. ✅ Валідувати дані (Zod)
4. ✅ Додавати try-catch в усіх async функціях
5. ✅ Використовувати middleware для обробки помилок
6. ✅ Не логувати паролі або токени
7. ✅ Використовувати Prisma для типобезпеки

### Frontend
1. ✅ Використовувати TypeScript
2. ✅ Додавати loading стани
3. ✅ Обробляти помилки користувацьки
4. ✅ Валідувати форми
5. ✅ Оптимізувати зображення
6. ✅ Використовувати lazy loading

### Git
1. ✅ Комітити часто та малими порціями
2. ✅ Писати зрозумілі коміт повідомлення
3. ✅ Не комітити `.env`, `node_modules`, логів
4. ✅ Використовувати `.gitignore`
5. ✅ Робити осмислені коміти

---

## 🔧 Troubleshooting

### Проблема: Prisma не підключається до БД
```bash
# Перевірити .env
# Перевірити чи запущено PostgreSQL
# Перевірити DATABASE_URL

npx prisma db push          # Синхронізувати схему без міграцій
```

### Проблема: Winston не створює файли
```bash
# Додати в logger.ts:
import fs from 'fs';
if (!fs.existsSync('logs')) fs.mkdirSync('logs', { recursive: true });
```

### Проблема: CORS помилки
```typescript
app.use(cors({
  origin: process.env.CORS_ORIGIN || 'http://localhost:3000',
  credentials: true
}));
```

### Проблема: TypeScript помилки
```bash
npx tsc --noEmit           # Перевірити без білду
npx tsc --init             # Ініціалізувати TypeScript
```

---

**💡 Нагадування: Завжди тестуй локально перед деплоєм!**

